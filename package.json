{
  "name": "rsm-gayrimenkul-crm",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check worker.js && node --check ui.js",
    "test": "node --test",
    "deploy": "npx wrangler deploy"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
